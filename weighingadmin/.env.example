# Backend API origin (no trailing slash, no /api suffix).
# Leave unset for local development — falls back to http://localhost:5001.
# Set to the deployed backend's real URL (e.g. Render) for a production build.
VITE_API_URL=
